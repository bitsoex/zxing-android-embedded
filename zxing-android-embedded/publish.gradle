apply plugin: 'maven-publish'

def libGroupId = 'com.bitso.android'
def libArtifactId = 'zxing-embedded'
def libVersion = '3.6.0'

task sourceJar(type: Jar) {
    from android.sourceSets.main.java.srcDirs
    classifier "sources"
}

publishing {
    repositories {
        maven {
            name = "GithubPackages"
            url = uri("https://maven.pkg.github.com/bitsoex/android-zxing-embedded")
            credentials {
                username = System.getenv('GITHUB_USER')
                password = System.getenv('GITHUB_TOKEN')
            }
        }
        maven {
            name = 'LocalMavenRepo'
            url = "file://${buildDir}/repo"
        }
    }
    publications {
        zxingEmbedded(MavenPublication) {
            groupId libGroupId
            artifactId libArtifactId
            version libVersion
            artifact("$buildDir/outputs/aar/${project.name}-release.aar")
            artifact(sourceJar)
        }
    }
}
